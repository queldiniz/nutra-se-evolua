services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    container_name: nutrase-frontend
    ports:
      - "3000:3000"
    restart: unless-stopped
